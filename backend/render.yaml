services:
  - type: web
    name: veri-rag-backend
    rootDir: ./backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend_src.main:app --host 0.0.0.0 --port $PORT"
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
